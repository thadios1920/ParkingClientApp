export class Car {
    constructor(
        public main:boolean,
        public serialNumber: string,
        public brand: string,
        public model: string,
        public color: string,
    ){

    }
}
